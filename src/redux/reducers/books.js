import { actionTypes } from '../actions/books';
const initialState = [];

export default function books(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
